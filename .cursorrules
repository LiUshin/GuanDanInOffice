# GuanDanInOffice

局域网掼蛋。代码在本目录，不是 workspace 根目录。

## 架构

- Room（`src/server/room.ts`）管座位、准备、模式、聊天。
- Match（`src/server/match.ts`）管从 2 打到 A 的整场，3 秒后自动下一局。
- Game（`src/server/game.ts`）管单局：发牌、进贡/还贡、出牌、技能、Bot 定时器。
- 牌型判定在 `src/shared/rules.ts`，Bot 在 `src/shared/bot.ts`。
- 客户端状态在 `src/client/useGame.ts`，牌桌在 `src/client/components/GameTable.tsx`。
- 座位 0/2 为一队，1/3 为一队。当前打的等级是庄家队等级。房主权限绑在座位 0，换座会把房主权交出去。

## 项目内规则（以 README 为准，和代码不一致时先问）

- 升级：双扣 +3，单扣 +2，保级 +1，封顶 A（14）。
- 进贡：双扣时末游给头游、三游给二游；单扣时末游给头游；头游和末游同队则不进贡。
- 抗贡：进贡方合计两张大王。
- 还贡后，进贡牌最大的人先出。
- 接风：一轮赢家已出完时，对家先出。
- 红心级牌是癞子。
- 牌型大小：四大天王 > 同花顺 > 普通炸弹（张数多的更大，张数相同比点数）> 其他牌型。同花顺大于任意张数的普通炸弹。
- 对局结束：某队等级到 A 且连胜 2 次。升到 A 的那一局算连胜的第一场。
- 还贡只能出 10 及以下，且不能是级牌或王。手里没有这种牌时，还逻辑点数最小的一张。
- 自动理牌在 `src/shared/arrange.ts`。从左到右：天王炸、同花顺、炸弹、钢板（两连三）、三连对、顺子、三带二、三张、对子、单张。同花顺优先占用能组成它的牌。只分组 `getHandType` 认得出的组合。点组名可以整组选中。同花顺视图按牌面点数分列，级牌留在自己的点数列里。

## 术语

- 三连对（334455）在代码里是 `HandType.Tube`，文案有的写成「钢板」。
- 钢板（333444）是 `HandType.Plate`，三连对是 `HandType.Tube`。桌面、历史、`getHandDescription` 都用这两个中文名。级牌显示走 `formatLevelRank`（11–14 → J/Q/K/A）。

## 已修

- 进贡先手、Bot 还贡、强制结束清定时器并 destroy、技能打光记名次、乐不思蜀在接风时消耗、出牌以服务器手牌为准。
- 同花顺大于所有普通炸弹。A2345 同花顺顶张是 5。JQKA + 癞子可以组成 10JQKA。
- 还贡限制见上。
- 默认手牌用 `arrangeHand` 分组。
- 断线后系统代打（出牌、进贡、还贡），重连后把控制权交还。房间状态带 `isDisconnected`。
- 整场结束或房主强制结束后清掉 Bot。座位 0 空了就把还在的玩家挪过去当房主。
- 双下名次用 `rankUnfinishedPlayers`：剩余牌少的是三游，牌数相同则离二游更近的是三游。
- 牌局左上显示「当前 打X / 我方 / 对方 / 庄」。结算和整场结束横幅同样用 `formatLevelRank`。自己回合「轮到你了」，别人「思考中」。自由出牌时「过」禁用。选中牌被服务端拒绝就留着，牌离开手牌才清。
- 普通播报（技能、抗贡、断线、重连、换模式、离座、强制结束）走 socket `notice`，蓝条。非法操作仍走 `error`，红条。整场结束是页面横幅，不再 `alert`。大厅注明按 `i` 摸鱼。牌桌没有 Debug 行。

## 尚未修

### 房间
- 重连只认名字。重连的 `gameState` 缺少技能、回合动作、模式、历史，而且先于 `roomState` 发出。
- 准备只能变成 true，不能取消。四人准备会自动开局。
- 房主是座位 0。开局前换到别的座位会把房主权交出去。
- 没有离开房间。同一个 socket 可以再 `joinRoom` 占第二个座位，也可以同时待在两个房间。
- 房间列表把断线的人算成空位，但座位仍被占着。空房间不删除。

### 对局进程
- 每一局都 `new Game()`，`currentRound` 从 0 再加一，所以历史里永远是「第 1 局」。

### Bot
- 自由出牌不走顺子、三连对、钢板，也不会主动出炸弹。手里只剩炸弹时会拆成三张。
- `findBeat` 对顺子、三连对、钢板是空的，只能用炸弹压，否则过牌。
- 自由出牌如果被服务端拒绝，不会改出别的牌，回合停住。
- 技能：手牌少于 15 张就会用五谷丰登。用技能后 1 秒和 `broadcastGameState` 的 1.5 秒会各排一次 Bot 行动，靠「还是不是自己的回合」挡掉第二次。

### 其他
- `getAllPossibleHandTypes` 每种张数最多一种解，万能牌选择弹层不会出现。
- 聊天气泡的定时器会被下一条消息清掉，上一条气泡会一直留着。

## UI 待优化（先问再改）

- 理牌组名 10px，点组名全选没有说明。手牌区 overflow 会裁掉选中上移。
- 聊天固定在右上，小屏会挡住右侧玩家。进贡牌没有高亮。

## 改动时注意

- 新的 `setTimeout` 都要走 `Game.registerTimeout`，对局结束和 `destroy` 时清掉。
- Match 那只跨局的 3 秒定时器不在 Game 的 timeout 列表里，要单独清。
- 出牌、进贡、还贡都以手牌里的 `card.id` 为准，不要用客户端对象上的 `isWild` / `rank`。
- 技能让某人牌数为 0 时，要走和正常出完一样的名次与终局判断。
